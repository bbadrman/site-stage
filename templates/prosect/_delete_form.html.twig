<form method="post" action="{{ path('app_prosect_delete', {'id': prosect.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prosect.id) }}">
    <button class="btn">Delete</button>
</form>
